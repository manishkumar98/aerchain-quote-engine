/**
 * Re-export the deterministic normalization engine from steps/step_2/
 * to maintain clean module organization while keeping standard application imports (@/lib/normalizationEngine) active.
 */
export * from '../../steps/step_2/normalizationEngine';
